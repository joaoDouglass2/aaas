{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    "baseUrl": ".",
    "paths": {
      "@/*": ["app/*"],              // 👉 Quando você importa com "@/algumaCoisa", o TypeScript vai olhar dentro da pasta app
      "@firebase/*": ["firebase/*"], // 👉 Quando você faz "@firebase/config", o TS vai olhar para "firebase/config"
      "@services/*": ["services/*"]  // 👉 Quando você faz "@services/api", o TS vai olhar para "services/api"
    }
  },
  "include": [
    "**/*.ts",
    "**/*.tsx",
    ".expo/types/**/*.ts",
    "expo-env.d.ts"
  ]
}
